import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "PT TOSADAH - Transforming Ideas into Digital Solutions",
  description: "PT TOSADAH is a leading technology company specializing in software development, digital transformation, and innovative solutions for businesses.",
  keywords: ["PT TOSADAH", "software development", "digital solutions", "technology", "innovation"],
};

export default function RootLayout({
  children,
}: Readonly<{ 
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}